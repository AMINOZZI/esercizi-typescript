//to install globaly typescript 
npm install -g typescript

//create js file app.js
//this command create a js file
tsc app.ts

//install node
npm init

// to install server dev lite with port http://localhost:3000
npm install --save-dev lite-server

//modify package.json in script object with this line 
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server"
  },

//lunch server with 
npm start

